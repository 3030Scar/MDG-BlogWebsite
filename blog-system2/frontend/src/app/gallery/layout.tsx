import React from "react";

export const metadata = {
  title: "这里先放着，过会删",
  description: "PaperGroup's photography and design gallery.",
};

export default function GalleryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
